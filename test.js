/**
 * Testing without using the discord.js api.
 */

const log = require("./log");
const debug = require("./debug");
var spnav = require("./spwikia-nav");

spnav.getPageInfo("social justice", function(title, url, desc, thumbnail) {
    
});