"use strict";

const guildSchema = require("./guild");
const userSchema = require("./user");
const scriptSchema = require("./script");

module.exports = {

    guildSchema,
    userSchema,
    scriptSchema
};
