/**
 * log.js
 * Desc: Utility module for logging info.
 * Deps: None
 */

"use strict"

function log(level, string, source = __filename) {

    //log levels
}

function logChat(message) {

    // file io
}