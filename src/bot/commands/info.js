"use strict"

const Command = require("../command");

const info = new Command("info", "a command for getting info various discord objects", "js", 0, "info", "command", 0, false, null, function(client, message, guildDoc){

});

module.exports = info;
